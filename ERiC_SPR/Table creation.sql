CREATE TABLE SLOC (
    `Descr. of Storage Loc.` VARCHAR(255),
    Plant INT,
    `Storage location` INT,
    Material INT PRIMARY KEY,
    `Material Description` VARCHAR(255),
    Unrestricted INT,
    `Base Unit of Measure` INT,
    `Value Unrestricted` INT,
    `Quality Inspection` INT,
    `Value in QualInsp.` INT,
    `Friendly Name` VARCHAR(255),
    Region VARCHAR(255),
    OPER INT
);

CREATE TABLE SLOC (
    `Descr. of Storage Loc.` VARCHAR(255),
    Plant INT,
    `Storage location` INT,
    Material INT PRIMARY KEY,
    `Material Description` VARCHAR(255),
    Unrestricted INT,
    `Base Unit of Measure` INT,
    `Value Unrestricted` INT,
    `Quality Inspection` INT,
    `Value in QualInsp.` INT,
    `Friendly Name` VARCHAR(255),
    Region VARCHAR(255),
    OPER INT
);
